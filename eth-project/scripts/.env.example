# Sepolia 測試網設置
QUICKNODE_URL="https://your-quicknode-http-url"
QUICKNODE_WS_URL="wss://your-quicknode-websocket-url"
PRIVATE_KEY="your-wallet-private-key-without-0x"
CONTRACT_ADDRESS=""

# 監控設置
MONITOR_ADDRESS=""

#從dash board拿
#或是一次性拿
#node -e "
#const { ethers } = require('ethers');
#const wallet = ethers.Wallet.createRandom();
#console.log('\n=== 新錢包資訊 ===');
#console.log('地址:', wallet.address);
#console.log('私鑰 (無0x):', wallet.privateKey.slice(2));
#console.log('助記詞:', wallet.mnemonic.phrase);
#console.log('\n將這些值複製到 .env 文件中：');
#console.log('PRIVATE_KEY=' + wallet.privateKey.slice(2));
#console.log('MONITOR_ADDRESS=' + wallet.address);
#"
